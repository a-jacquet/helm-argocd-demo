<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Appender console : logs lisibles pour kubectl logs / développement local -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender Logstash : logs JSON envoyés en TCP -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <!-- LOGSTASH_HOST = variable d'env injectée par K8s, fallback localhost pour dev -->
        <destination>${LOGSTASH_HOST:-localhost}:${LOGSTASH_PORT:-5044}</destination>
        <keepAliveDuration>5 minutes</keepAliveDuration>
        <!-- Si Logstash est injoignable, ne bloque pas l'app -->
        <reconnectionDelay>10 second</reconnectionDelay>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>
                {"service":"${spring.application.name:-demo-app}","env":"${spring.profiles.active:-local}"}</customFields>
            <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                <maxDepthPerThrowable>30</maxDepthPerThrowable>
                <maxLength>2048</maxLength>
                <rootCauseFirst>true</rootCauseFirst>
            </throwableConverter>
        </encoder>
    </appender>

    <!-- Niveau root : INFO vers console ET Logstash -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="LOGSTASH" />
    </root>

    <!-- Réduire le bruit des librairies -->
    <logger name="org.hibernate.SQL" level="DEBUG" />
    <logger name="com.demo.demo_app" level="DEBUG" />

</configuration>