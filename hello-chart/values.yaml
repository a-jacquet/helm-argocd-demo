replicaCount: 1

image:
  repository: nginx
  tag: alpine
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

containerPort: 80

liquibase:
  url: jdbc:postgresql://postgres-postgresql.demo.svc.cluster.local:5432/demodb
  username: demo
  password: demo123
